# TradingView Portfolio Backtest Framework Workspace Rules

- **Command Optimization**: Always prefix shell commands with `rtk` (e.g., `rtk git status`, `rtk pytest`, `rtk ls`, etc.) to minimize token consumption and compress system outputs.
